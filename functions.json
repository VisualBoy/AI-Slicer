{
  "functions": [
    {
      "name": "control_lights",
      "description": "Control the lights in the room",
      "parameters": {
        "type": "object",
        "properties": {
          "state": {
            "type": "boolean",
            "description": "True to turn lights on, False to turn them off"
          }
        },
        "required": ["state"]
      }
    },
    {
      "name": "web_search",
      "description": "Search the web for real-time information",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The search query to look up"
          }
        },
        "required": ["query"]
      }
    },
    {
      "name": "toggle_silent_mode",
      "description": "Toggle silent mode on or off",
      "parameters": {
        "type": "object",
        "properties": {
          "state": {
            "type": "boolean",
            "description": "True to enable silent mode, False to disable it"
          }
        },
        "required": ["state"]
      }
    },
    {
      "name": "slice_model",
      "description": "Slice a 3D model file (STL, 3MF, etc.) using PrusaSlicer to generate G-code.",
      "parameters": {
        "type": "object",
        "properties": {
          "file_path": {
            "type": "string",
            "description": "The name of the 3D model file (e.g., 'benchy.stl'), its number from the list, OR the full path to the file."
          },
          "output_path": {
            "type": "string",
            "description": "Optional. The full path where the G-code file should be saved. If not provided, a default name will be used."
          }
        },
        "required": ["file_path"]
      }
    },
    {
      "name": "list_stl_files",
      "description": "Lists all .stl, .3mf, and .obj files found in the default 3D objects folder.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "set_preference",
      "description": "Sets a specific user preference, like default layer height or infill.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "The name of the preference to set (e.g., 'default_layer_height')."
          },
          "value": {
            "type": "string",
            "description": "The value to set for the preference (will be converted if needed)."
          }
        },
        "required": ["key", "value"]
      }
    },
    {
      "name": "view_gcode",
      "description": "Opens a specified .gcode file using the Prusa G-code Viewer.",
      "parameters": {
        "type": "object",
        "properties": {
          "gcode_file_path": {
            "type": "string",
            "description": "The name of the .gcode file (e.g., 'benchy.gcode') located in the default folder, or the full path to the file."
          }
        },
        "required": ["gcode_file_path"]
      }
    }
    {
      "name": "octoprint_list_files",
      "description": "Lists files present on OctoPrint.",
      "parameters": {
        "type": "object",
        "properties": {
          "location": {
            "type": "string",
            "description": "Optional. The location to list files from ('local' or 'sdcard'). Defaults to 'local'."
          },
          "recursive": {
            "type": "boolean",
            "description": "Optional. Whether to list files recursively. Defaults to true."
          }
        }
      }
    },
    {
      "name": "octoprint_list_slicing_profiles",
      "description": "Lists slicing profiles available for a given slicer in OctoPrint.",
      "parameters": {
        "type": "object",
        "properties": {
          "slicer_name": {
            "type": "string",
            "description": "The name of the slicer (e.g., 'cura', 'slic3r')."
          }
        },
        "required": ["slicer_name"]
      }
    },
    {
      "name": "octoprint_start_print",
      "description": "Starts printing an existing G-code file on OctoPrint.",
      "parameters": {
        "type": "object",
        "properties": {
          "file_path_on_octoprint": {
            "type": "string",
            "description": "The path of the G-code file on OctoPrint (e.g., 'folder/file.gcode')."
          }
        },
        "required": ["file_path_on_octoprint"]
      }
    },
    {
      "name": "octoprint_slice_model",
      "description": "Slices an STL/3MF model on OctoPrint using a specified slicer and profile.",
      "parameters": {
        "type": "object",
        "properties": {
          "file_path_on_octoprint": {
            "type": "string",
            "description": "The path of the model file (e.g., 'model.stl') on OctoPrint."
          },
          "slicer_name": {
            "type": "string",
            "description": "The name of the slicer to use (e.g., 'cura')."
          },
          "slicing_profile_key": {
            "type": "string",
            "description": "The key of the slicing profile to use."
          },
          "printer_profile_key": {
            "type": "string",
            "description": "Optional. The key of the printer profile for slicing. Defaults to '_default'."
          },
          "output_gcode_name": {
            "type": "string",
            "description": "Optional. Desired name for the resulting G-code file. If not provided, OctoPrint will generate one."
          },
          "print_after_slice": {
            "type": "boolean",
            "description": "Optional. If true, OctoPrint will start printing the generated G-code after successful slicing. Defaults to false."
          }
        },
        "required": ["file_path_on_octoprint", "slicer_name", "slicing_profile_key"]
      }
    }
  ]
}